[behave]
# Tell behave where feature files are located
paths = src/advent

# Tell behave where your steps directory lives
steps_dir = src/advent/steps

# logging
logging_level = DEBUG
log_capture = false
stderr_capture = false
stdout_capture = false